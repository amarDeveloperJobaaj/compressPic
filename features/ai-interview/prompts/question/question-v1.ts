/**
 * Question generation prompt — v1 placeholder.
 *
 * Implemented in Phase 5 (AI Question Engine, master spec §23–25).
 * The file exists now so the prompts/ versioning structure is stable
 * (prompts/<type>/<type>-v<N>.ts) and future phases only fill it in.
 */

export const QUESTION_PROMPT_VERSION = 1;

export const QUESTION_SYSTEM_PROMPT = /* placeholder — Phase 5 */ "";

export function buildQuestionUserPrompt(_context: unknown): string {
  void _context; // placeholder — filled in Phase 5
  return "";
}
